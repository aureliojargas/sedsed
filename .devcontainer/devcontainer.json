// https://aka.ms/devcontainer.json
{
    "name": "replace dev",
    "image": "ghcr.io/aureliojargas/devcontainer",
    "remoteUser": "vscode",
    // Those symlinks are need by the `make test` command
    "postCreateCommand": "sudo ln -s /usr/bin/python3 /usr/bin/python; sudo ln -s /usr/bin/sed /usr/bin/gsed"
}
